# ── Backend Server URL ────────────────────────────────────────────────────────
# URL of the deployed backend (Railway).
# Leave blank for local dev (auto-detects same hostname, port 3000).
# In production, set this in Vercel: Project > Settings > Environment Variables.
VITE_SERVER_URL=https://your-backend.railway.app
